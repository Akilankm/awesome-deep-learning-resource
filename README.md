# ANN on MNIST — finalization staging

This temporary staging commit is replaced by CI with the final canonical branch contents and rendered notebooks. Do not use this intermediate commit as the learning artifact.
